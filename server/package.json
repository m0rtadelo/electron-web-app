{
  "name": "electron-web-app-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "start": "node dist/server.js",
    "build": "npm run build:bundle && npm run build:copy",
    "build:bundle": "tsc",
    "build:copy": "cp ./package.json dist",
    "build:watch": "tsc --watch",
    "run": "npm i && node /home/node/app/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "typescript": "^4.2.4"
  }
}
